/**
 * AIRPORT - Módulo Perfil (placeholder para Fase 2)
 */
export const ProfileModule = {
  async init(container) {
    container.innerHTML = '<p class="page-placeholder">Perfil — Módulo en desarrollo.</p>';
  },
};
export default ProfileModule;
