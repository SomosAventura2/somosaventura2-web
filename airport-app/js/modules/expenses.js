/**
 * AIRPORT - Módulo Gastos (placeholder para Fase 2)
 */
export const ExpensesModule = {
  async init(container) {
    container.innerHTML = '<p class="page-placeholder">Gastos — Módulo en desarrollo.</p>';
  },
};
export default ExpensesModule;
