/**
 * AIRPORT - Módulo Pedidos (placeholder para Fase 2)
 */
export const OrdersModule = {
  async init(container) {
    container.innerHTML = '<p class="page-placeholder">Pedidos — Módulo en desarrollo.</p>';
  },
};
export default OrdersModule;
